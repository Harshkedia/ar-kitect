machine:
  services:
    - docker

dependencies:
  override:
    - echo "no deps"

test:
  post:
    - echo "yay - no tests"

deployment:
  production:
    branch: master
    commands:
      - docker build -t ar-kitect .
      - docker login --email=_ --username=_ --password=$HEROKU_TOKEN registry.heroku.com
      - docker tag ar-kitect:latest registry.heroku.com/ar-kitect/web
      - docker push registry.heroku.com/ar-kitect/web
